#include <stdio.h>
#include <math.h>
#include <process.h>
#define f(x) l*pow(x,3)+m*pow(x,2)+n*pow(x,1)+0
float
void inputCoeff(float l, float m, float n, float o, float a, float b)
{
    printf("Enter the coefficients of the equation : ");
    scanf("%f",l);
    scanf("%f",m);
    scanf("%f",n);
    scanf("%f",o);
    printf("\nEnter the 2 initial guesses : ")
    scanf("%f",&a);
    scanf("%f",&b);

}
void main ()
{
    float l,m,n,o,a,b,mid,fa,fb,fmid;


}
